apiVersion: v1
kind: Service
metadata:
  name: {{ include "omni-router.fullname" . }}
  labels:
    {{- include "omni-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "omni-router.selectorLabels" . | nindent 4 }}
